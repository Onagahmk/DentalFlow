package com.rodrigodecastro.dentalflow.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de cores inspirada na referência (Tons de Roxo/Azul Suave)

// Cores para o Tema Claro (Light Theme)
val AppPrimary = Color(0xFF6A79E8)       // Roxo/Azul principal para botões e destaques
val AppOnPrimary = Color(0xFFFFFFFF)     // Texto branco para usar sobre a cor primária
val AppPrimaryContainer = Color(0xFFE0E0FF) // Um tom bem claro para containeres relacionados
val AppOnPrimaryContainer = Color(0xFF2432A3) // Texto escuro para usar sobre o container primário

val AppSecondary = Color(0xFF5A5D72)      // Cor secundária mais neutra
val AppOnSecondary = Color(0xFFFFFFFF)     // Texto sobre a cor secundária
val AppSecondaryContainer = Color(0xFFDEE1F9) // Container da cor secundária
val AppOnSecondaryContainer = Color(0xFF171B2C) // Texto sobre o container da cor secundária

val AppTertiary = Color(0xFF75546F)       // Cor terciária para outros destaques
val AppOnTertiary = Color(0xFFFFFFFF)      // Texto sobre a cor terciária
val AppTertiaryContainer = Color(0xFFFFD7F0) // Container da cor terciária
val AppOnTertiaryContainer = Color(0xFF2C122A)  // Texto sobre o container da cor terciária

val AppError = Color(0xFFBA1A1A)         // Cor de erro padrão
val AppOnError = Color(0xFFFFFFFF)       // Texto sobre a cor de erro

val AppBackground = Color(0xFFF9F9FF)    // Fundo principal do App (um branco levemente azulado)
val AppOnBackground = Color(0xFF1A1B21)   // Texto principal sobre o fundo

val AppSurface = Color(0xFFFFFFFF)       // Cor de superfície para os Cards (branco puro)
val AppOnSurface = Color(0xFF1A1B21)    // Texto sobre os cards
val AppSurfaceVariant = Color(0xFFE3E1EC)   // Variante da cor de superfície
val AppOnSurfaceVariant = Color(0xFF46464F) // Texto sobre a variante

val AppOutline = Color(0xFF777680)        // Cor para bordas

// Cores para o Tema Escuro (Dark Theme) - uma adaptação simples
val AppDarkPrimary = Color(0xFFBFC2FF)
val AppDarkOnPrimary = Color(0xFF3C439E)
val AppDarkBackground = Color(0xFF1C1B2A) // Fundo escuro azulado/roxo
val AppDarkSurface = Color(0xFF2C2D3A)   // Superfície dos cards um pouco mais clara que o fundo
